/******************************************************	StreamFile.java: class for handling i/o from parseable text files*	Can output to BufferedReader or StreamTokenizer*	16.08.2012*	David P. Leader******************************************************/import java.io.*;public class StreamFile{	private InputStreamReader inReader;	private StreamTokenizer inTokenizer;	private BufferedReader reader;		public StreamFile(String filePath, boolean tokOut)	{		openFile(filePath, tokOut);	}	/***************************************************** *	Gets InputStream, gives it to an InputStreamReader *  and then to a Stream Tokenizer if tokOut is true, *	else to a BufferedReader ******************************************************/	private void openFile(String filePath, boolean tokOut)    { 	   	    	InputStream inStream = getClass().getResourceAsStream(filePath);		try 		{						inReader = new InputStreamReader(inStream);						if(inReader == null)			{				System.out.println("inStream == null");				inReader.close();			}			else			{				if(tokOut)				{					inTokenizer = new StreamTokenizer(inReader);				}				else				{					reader = new BufferedReader(inReader);				}			}							}		catch (Exception e)		{ 			String message = e.toString();							System.out.println("openFile exception: " + message);		}    }        // Returns the StreamTokenizer    public StreamTokenizer getStream()    {		return inTokenizer;    }    // Returns the BufferedReader    public BufferedReader getReader()    {    	return reader;    }        // Closes the InputStream    public void closeFile()    {    	try 		{	    	if (inReader != null)	    	{		    	inReader.close();		    }		}		catch (Exception x)		{			String message = x.toString();							System.out.println("closeFile exception: " + message);		}    }						}